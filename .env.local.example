# Optional: enables the live Gemini path for Explore.
# Get a free key at https://aistudio.google.com — then: cp .env.local.example .env.local
# Without a key, Explore runs entirely on the built-in pattern matcher.
GEMINI_API_KEY=
